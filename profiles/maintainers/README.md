# Maintainer reference profiles

These profiles are public examples from project maintainers. They demonstrate the generic schema with real-world configuration choices.

They are reference implementations, not recommended defaults. The browser builder starts from anonymous presets instead.

A maintainer profile must not require special-case code in the schema, renderer, linter, browser builder, or tests. If a feature only works for one named profile, it does not belong in the generic core.
